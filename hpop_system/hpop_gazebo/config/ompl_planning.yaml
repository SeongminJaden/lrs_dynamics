# OMPL Planning Configuration for MoveIt2
# Configures sampling-based motion planners

planning_plugin: ompl_interface/OMPLPlanner
request_adapters: >-
    default_planner_request_adapters/AddTimeOptimalParameterization
    default_planner_request_adapters/ResolveConstraintFrames
    default_planner_request_adapters/FixWorkspaceBounds
    default_planner_request_adapters/FixStartStateBounds
    default_planner_request_adapters/FixStartStateCollision
    default_planner_request_adapters/FixStartStatePathConstraints

start_state_max_bounds_error: 0.1

arm:
  planner_configs:
    - RRTConnect
    - RRT
    - RRTstar
    - PRM
    - EST
    - BiEST

  # Default planner
  default_planner_config: RRTConnect

  # Projection evaluator (for efficient sampling)
  projection_evaluator: joints(joint1,joint2,joint3)
  longest_valid_segment_fraction: 0.005

planner_configs:
  RRTConnect:
    type: geometric::RRTConnect
    range: 0.0  # Max motion added to tree (0 = auto)
  RRT:
    type: geometric::RRT
    range: 0.0
    goal_bias: 0.05
  RRTstar:
    type: geometric::RRTstar
    range: 0.0
    goal_bias: 0.05
    delay_collision_checking: true
  PRM:
    type: geometric::PRM
    max_nearest_neighbors: 10
  EST:
    type: geometric::EST
    range: 0.0
    goal_bias: 0.05
  BiEST:
    type: geometric::BiEST
    range: 0.0
